[project]
name = "trajectory_eval"
version = "0.1.0"
description = "A model-agnostic toolkit for auditing trajectory-prediction evaluation."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.21",
    "scipy>=1.7",
    "matplotlib>=3.4",
    "pandas>=1.3",
]

[tool.uv]
package = false
